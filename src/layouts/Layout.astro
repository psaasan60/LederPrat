---
import '../styles/global.css';
import Nav from '../components/Nav.astro';
import Footer from '../components/Footer.astro';

interface Props {
	title?: string;
	description?: string;
}

const {
	title = 'Paul Aasan | Lederprat',
	description = 'Din sparringspartner som leder — erfaren rådgivning for ledere i små og mellomstore bedrifter.',
} = Astro.props;
---

<!doctype html>
<html lang="no">
	<head>
		<meta charset="utf-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1" />
		<meta name="description" content={description} />
		<title>{title === 'Paul Aasan | Lederprat' ? title : `${title} · Lederprat`}</title>
		<link rel="icon" type="image/png" href="/favicon.png" />
	</head>
	<body>
		<Nav />
		<main>
			<slot />
		</main>
		<Footer />
	</body>
</html>
